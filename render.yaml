services:
  - type: web
    name: tender-bot
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python Bot_Render.py
    envVars:
      - key: BOT_TOKEN
        scope: run
      - key: PROXY_URL
        scope: run
